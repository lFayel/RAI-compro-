#include<stdio.h>
int main();
{
    int sum,avg,i,num;
    for(i=1;i<=10;i++); {
        printf("%d.Enter the number:",i);
        scanf("%d",&i)
        sum = sum + num;
        num = 0;
    }
    avg = sum/i;
    printf("total sum is: %d\n")
    printf("Average is: %d\n")
    return 0;
}